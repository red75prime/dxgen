use winapi::*;
use core::*;
use create_device::*;
use utils::*;
use dx_safe::*;
use dx_safe::structwrappers::*;
use std::ptr;
use std::io;
use std::io::prelude::*;
use std::fs::File;
use std::str;
use std::slice;



struct Reductor {
}
